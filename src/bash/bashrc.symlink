#!/usr/bin/env bash

if [ -a $HOME/.dotrc ]; then
    source $HOME/.dotrc
fi

if [ -z "$DOTFILES" ]; then
    export DOTFILES="$HOME/.dotfiles"
fi

if [ -f "$DOTFILES/src/setup" ]; then
    source "$DOTFILES/src/setup"
else
    echo "Cannot source '$DOTFILES/src/setup'." >&2
fi
